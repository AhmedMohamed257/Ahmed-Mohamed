<section class="hiring-grid">

  <div class="page-width">

    <div class="hg-header">
      <h2>{{ section.settings.heading }}</h2>
    </div>

    <div class="hg-products">

      {% for block in section.blocks %}
      {% assign product = block.settings.product %}
      <div class="hg-card">

        <div class="hg-image">

          {% if block.settings.image %}
            <img
              src="{{ block.settings.image | image_url: width: 700 }}"
              alt="{{ product.title }}"
              loading="lazy"
              width="{{ block.settings.image.width }}"
              height="{{ block.settings.image.height }}">
          {% endif %}

          <button
              class="hg-plus"
              type="button"
              data-popup="popup-{{ block.id }}"
          >  

              <img
                  src="{{ 'plus-icon.svg' | asset_url }}"
                  alt=""
                  width="42"
                  height="42"
            >

          </button>

        </div>

      </div>

      <!-- Popup -->

      <div class="hg-popup" id="popup-{{ block.id }}">

        <div class="hg-popup-overlay"></div>

        <div class="hg-popup-box">

          <button class="hg-close">&times;</button>

          <div class="hg-popup-grid">

            <div class="hg-popup-image">

              {% if product.featured_image %}
                <img
                src="{{ product.featured_image | image_url: width:500 }}"
                width="250"
                height="300"
                alt="{{ product.title }}">
              {% endif %}

            </div>

            <div class="hg-popup-content">

              <h3>{{ block.settings.title }}</h3>

              <p class="hg-popup-price">
                {{ product.price | money }}
              </p>

              <div class="hg-popup-description">
                {{ product.description }}
              </div>

              <label>Color</label>

              <div class="hg-colors">

                <button>{{ block.settings.color1 }}</button>

                <button>{{ block.settings.color2 }}</button>

              </div>

              <label>Size</label>

              <select>

                <option>{{ block.settings.size_placeholder }}</option>

                <option>S</option>

                <option>M</option>

                <option>L</option>

                <option>XL</option>

              </select>

              <a href="{{ block.settings.button_link }}" class="hb-btn hb-btn--black">

                <span class="hb-btn__fill"></span>

                <span class="hb-btn__label">
                  {{ block.settings.button_text }}
                </span>

                <span class="hb-btn__arrow">
                  <img src="{{ 'icon-arrow.svg' | asset_url }}" alt="">
                </span>

              </a>

            </div>

          </div>

        </div>

      </div>

      {% endfor %}

    </div>

  </div>

</section>

{% schema %}
{
  "name":"Hiring Grid",
  "tag":"section",
  "class":"section-hiring-grid",

  "settings":[

    {
      "type":"text",
      "id":"heading",
      "label":"Heading",
      "default":"Tisso vision in the wild"
    }

  ],

  "blocks":[

    {
      "type":"product",

      "name":"Product",

      "settings":[

        {
          "type":"image_picker",
          "id":"image",
          "label":"Grid Image"
        },

        {
          "type":"image_picker",
          "id":"popup_image",
          "label":"Popup Image"
        },

        {
          "type":"text",
          "id":"title",
          "label":"Title",
          "default":"Orange Wide Leg"
        },

        {
          "type":"text",
          "id":"price",
          "label":"Price",
          "default":"980,00€"
        },

        {
          "type":"richtext",
          "id":"description",
          "label":"Description",
          "default":"<p>This one-piece swimsuit is crafted from jersey featuring an allover micro Monogram motif in relief.</p>"
        },

        {
          "type":"text",
          "id":"color1",
          "label":"First Color",
          "default":"White"
        },

        {
          "type":"text",
          "id":"color2",
          "label":"Second Color",
          "default":"Black"
        },

        {
          "type":"text",
          "id":"size_placeholder",
          "label":"Size Placeholder",
          "default":"Choose your size"
        },

        {
          "type":"text",
          "id":"button_text",
          "label":"Button",
          "default":"ADD TO CART"
        },

        {
          "type":"url",
          "id":"button_link",
          "label":"Button Link"
        }

      ]

    }

  ],

  "max_blocks":6,

  "presets":[
    {
      "name":"Hiring Grid"
    }
  ]

}
{% endschema %}